VITE_SENTRY_DSN=

# Deepgram API key. Powers both the nova-3 live transcription recorder (Voice tab)
# and the conversational Voice Agent copilot (Copilot tab: nova-3 + gpt-4o-mini + aura-2).
VITE_DEEPGRAM_API_KEY=

# Anthropic API key used by the recorder to summarize transcripts into design changes
VITE_ANTHROPIC_API_KEY=
