# Secrets — never commit
.env
.env.*
!.env.example
ui/frontend/.env

# Python
__pycache__/
*.pyc
.venv/

# Loop run artifacts
results/loop_runs/
results/experiments/
results/compression/
results/ui_runs/
results/ui_design_runs/
results/procurement_cache/

# Large docs / binaries / scratch (not for the shared repo)
*.pdf
docs/
mock.png

# Local tooling / agent skills / editor state (not for the shared repo)
.agents/
.claude/
skills-lock.json

# Node / frontend build deps & caches (platform-specific — never commit; run `npm install`)
node_modules/
ui/frontend/.vite/
ui/frontend/dist/
.DS_Store
*.log# Secrets — never commit
.env
.env.*
!.env.example
ui/frontend/.env

# Python
__pycache__/
*.pyc
.venv/

# Loop run artifacts
results/loop_runs/
results/experiments/
results/compression/
results/ui_runs/
results/ui_design_runs/
results/procurement_cache/

# Large docs / binaries / scratch (not for the shared repo)
*.pdf
docs/
mock.png

# Local tooling / agent skills / editor state (not for the shared repo)
.agents/
.claude/
skills-lock.json

# Node / frontend build deps & caches (platform-specific — never commit; run `npm install`)
node_modules/
ui/frontend/.vite/
ui/frontend/dist/
.DS_Store
*.log
results/procurement_runs/

# Keep the project preprint tracked (overrides *.pdf) so the README link resolves.
!paper/main.pdf
