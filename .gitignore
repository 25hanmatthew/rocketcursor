# Secrets — never commit
.env
.env.*
!.env.example

# Python
__pycache__/
*.pyc
.venv/

# Loop run artifacts
results/loop_runs/
results/experiments/
results/compression/
results/ui_runs/
results/ui_design_runs/

# Large docs / binaries / scratch (not for the shared repo)
*.pdf
docs/
mock.png

# Local tooling / agent skills / editor state (not for the shared repo)
.agents/
.claude/
skills-lock.json

# Node / frontend build deps & caches (platform-specific — never commit; run `npm install`)
node_modules/
ui/frontend/.vite/
.DS_Store
